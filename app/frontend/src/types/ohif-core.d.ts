// Ambient declaration for the untyped `@ohif/core` package (no bundled types).
// Only `Types` is consumed (ohif.config.ts), used loosely for the DICOMweb
// data source config shape.
declare module '@ohif/core';
